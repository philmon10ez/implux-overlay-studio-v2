# Build output (Remix); deploy builds on Railway or via CI
/build

# Dependencies — install on Railway during build (do not commit)
node_modules/

# Local Prisma / tooling cache
# Env (keep .env.example tracked)
.env
!.env.example
